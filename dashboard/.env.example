# Helios Sentinel · Frontend Environment Config
# Copy to client/.env and fill in your values.

# Backend API base URL — no trailing slash
# Development (Vite proxy handles it automatically — leave blank or set to empty)
VITE_API_URL=
